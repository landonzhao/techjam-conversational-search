# Core demo and application dependencies
Flask>=3.0,<4.0
python-dotenv>=1.0,<2.0

# Test runner
pytest>=8.0,<9.0

# Optional dense-retrieval / embedding build dependencies.
# The agent automatically falls back to BM25 when the embedding cache or model is unavailable.
numpy>=1.24,<3.0
sentence-transformers>=3.0,<6.0

# Optional LLM and learned-ranking tooling.
# These are not required for the offline demo path.
google-genai>=1.0,<2.0
scikit-learn>=1.3,<2.0
